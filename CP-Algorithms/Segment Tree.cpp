// Saving the entire subarrays in each vertex
    // Find the smallest number greater or equal to a specified number no modification
        // Use array and merging with two pointers O(log(n))
    // Find the smallest number greater or equal to a specified number with modification
        // Use multiset to erase previous element and add new elemement  O(log^2(n))
    // Find the smallest number greater or equal to a specified number. Acceleration with "fractional cascading"
        // Read again
    //
    